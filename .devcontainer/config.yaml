database:
  path: .devcontainer/data/data.db
  migrations_path: backend/internal/migrations

log:
  level: DEBUG

server:
  port: 8081
  proxy:
    enable: true
    hostname: localhost
    port: 5173
